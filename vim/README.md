# My Vim config for C/C++ and Python

### Installation

1. Install vim-plug
2. :PlugInstall
3. Install YouCompleteMe with `python3 install.py --clangd-completer` (of use `--all` for all languages supporting, RTFM) 
